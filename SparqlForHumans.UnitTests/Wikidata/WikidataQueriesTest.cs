using System;
using System.Collections.Generic;
using System.Text;

namespace SparqlForHumans.UnitTests.Wikidata
{
    public class WikidataQueriesTest
    {
        

        //protected override async Task OnInitializedAsync()
        //{
        //    var endpoint = new CustomSparqlEndPoint(new Uri("https://query.wikidata.org/sparql"));
        //    //Make a SELECT query against the Endpoint
        //    var results = endpoint.QueryWithResultSet(WithPrefix().ToString());
        //    foreach (var result in results)
        //        Console.WriteLine(result.ToString());
    }
}
